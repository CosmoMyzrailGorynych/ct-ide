head
    title ct.js
    meta(charset='utf-8')
    meta(name='description', content='IDE')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    meta(
        http-equiv="Content-Security-Policy"
        content="connect-src https: file: ipc: socket: ws://localhost:* ws://127.0.0.1:* http://localhost:* http://127.0.0.1:* blob:; script-src https: socket: http://localhost:* http://127.0.0.1:* 'unsafe-eval' 'unsafe-inline' blob:; img-src https: data: file: blob: http://localhost:* http://127.0.0.1:*; "
    )

    style.
        #theLoadingScreen {
            position: fixed;
            font-family: sans-serif;
            left: 0;
            right: 0;
            top: 0;
            bottom: 0;
            line-height: 100vh;
            text-align: center;
            z-index: 50;
            background: #446adb;
            color: #fff;
            font-size: 4vw;
            font-weight: 300;
            animation: loadingPulse 1s infinite;
        }
        #theLoadingScreen.whooshout {
            animation: 0.5s ease-in 1 forwards whooshout;
        }
        @keyframes loadingPulse {
            0%, 100% {
                background: #446adb;
            }
            50% {
                background: #5144db;
            }
        }
        @keyframes whooshout {
            0% {
                opacity: 1;
                border-radius: 0;
                transform: scale(1);
                pointer-events: none;
            }
            100% {
                opacity: 0;
                border-radius: 50vmin;
                transform: scale(0);
            }
        }
    link(href='/data/bundle.css' rel='stylesheet' lazyload)
    link(href='/data/themeDay.css' id="themeCSS" rel='stylesheet')
