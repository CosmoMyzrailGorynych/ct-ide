content-editor
    map-editor .aNiceTable.dense
        padding 0.25rem .5rem .5rem .5rem
        &:first-child
            padding @padding
        td
            padding 0 !important
        th
            padding 0 0 0.35rem
        input[type="text"], input[type="number"], select
            border-radius 0
            margin 0 -1px -1px 0
        .anActionableIcon
            margin-left 0.5rem
        button
            margin-top 0.5rem
